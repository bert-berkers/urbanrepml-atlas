figure: fig_patch_mean_centering
generated: '2026-07-17T01:09:51+00:00'
regenerated_from: scripts/mpc/report/fidelity_audit_data_figures.py
type: data-backed (real run artifacts)
git_commit_at_build: 26393f93e680c02da181a5c2f41b6683be194ea0
builder_commit: a497e79fbfcc15cdb86385102b434fb4a24ec765
commit_then_run_note: "git_commit_at_build (HEAD at render) CONTAINS this builder\
  \ \u2014 last touched by a497e79, an ancestor of HEAD. Commit-then-run (artifact-provenance\
  \ rule c) satisfied; the earlier untracked-builder self-heal is complete."
inputs:
- path: scripts/mpc/mnist/glimpse.py
  role: glimpse builder (build_glimpse / extract_patch / pool_to_s)
  sha256: ed0e4dde260af0cc32038ae2e3e31f293a6be76ac97e81ce237babe10a96a946
- path: data/mnist/MNIST/raw
  role: MNIST test split (torchvision)
  note: 'test image #3, digit 0; loaded via probe.load_mnist'
font_used: Calibri
font_target: Aptos
font_fallback_chain: Aptos (target, not installed) -> Calibri -> DejaVu Sans
font_substitution_note: Aptos (house target) not installed on this box; rendered with
  Calibri (closest available humanist sans, same substitution as the schematic builder).
palette: "INK/STEEL/AMBER/SIGNAL/GO/BLUE shared with scripts/mpc/report/fidelity_audit_figures.py.\
  \ Entity hues: control=SIGNAL red, meancenter=BLUE, cell3=GO green. CVD-validated\
  \ (dataviz validate_palette.js, light mode): red/blue \u0394E 20.5 protan / 28.4\
  \ normal; {red,blue,green} trio passes with red<->green in the 6-8 CVD band, legal\
  \ with the direct-label + distinct-marker secondary encoding these charts carry."
description: 'What the model sees at one fixation: real MNIST glimpse patches from
  glimpse.build_glimpse, raw [0,1] (top, sequential Oranges) vs per-patch mean-centered
  (bottom, shared symmetric RdBu_r diverging scale). Left: the source digit with the
  fixation and the three nested windows (4 foveal 8x8, parafoveal 16x16, peripheral
  24x24). Per-view DC (raw mean) annotated on the top row; per-view mean = +/-0.0000
  on the centered row shows the DC removed exactly by construction (what --mean-center-patches
  feeds the model).'
seed: 42
determinism: digit = argmax total-ink over first 12 test images (#3, label 0); fixation
  = seeded-policy candidate (seed 42) maximising 8x8 foveal ink over 256 draws ->
  (x=13, y=9). Chosen for legibility; the mean-centering identity holds at any fixation.
shared_centered_scale: symmetric +/-0.7700 across all 6 centered views
raw_per_view_DC:
  F1: 0.4209
  F2: 0.545
  F3: 0.5841
  F4: 0.5022
  Pa: 0.3119
  Pe: 0.2169
centered_per_view_mean:
  F1: 0.0
  F2: -0.0
  F3: -0.0
  F4: -0.0
  Pa: -0.0
  Pe: 0.0
anchors:
  mechanism: glimpse.py:117-121 _finalize (per-patch per-image DC removal when mean_center=True);
    README:189 (mean-centering forces input DC ~ 0 by construction); README:168 diagnostics
    (input_dc_mean large for raw, ~0 for mean-centered)
